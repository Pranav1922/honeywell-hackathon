"""Closed-loop tests: the loop completes a horizon, persists every timestep,
honours cooperative stop, and falls back to the baseline controller when a
controller raises.

Implemented alongside `app/loop.py` in Milestone 1.
"""
