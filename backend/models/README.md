# backend/models/

EnergyPlus building models — hackathon deliverable 2.

| Path | Purpose |
|---|---|
| `baseline/` | The unmodified reference building and its weather file. |
| `generated/` | Modified `.idf` variants written during runtime evaluation. |

Populated in Milestone 4. Nothing here is required for Milestones 1-3, which run
against the toy simulator.
